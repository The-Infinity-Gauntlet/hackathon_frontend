/*  import { injectable, inject } from 'tsyringe'
import { Api } from '@/@core/services/api'

import { BaseRepository } from '@/@templates/repositories/BaseRepository'

@injectable()
export class floodCameraMonitoringRepository extends BaseRepository<IUser> {
  constructor(@inject('ApiService') api: Api) {
    super(api, 'flood_monitoring/')
  }
}

*/
